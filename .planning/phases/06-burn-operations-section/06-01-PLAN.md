---
phase: 06-burn-operations-section
plan: 01
title: Build BurnOperations.tsx and wire into War Room page
goal: Burn Operations section live in /war-room with summary stats, transaction table, and INCINERATED visual treatment
requirements: [R3, R10]
dependencies: [Phase 5 complete, useBurns hook, /api/burns route]
estimated_tasks: 3
---

# Plan 06-01: BurnOperations Component + Page Wiring

## Goal
Build `BurnOperations.tsx` — a client component rendering burn statistics and transaction history with an "INCINERATED" fire-themed visual treatment. Wire it into the war-room page below TreasuryIntel.

## Context Files
```
READ src/components/dashboard/TreasuryIntel.tsx   — pattern reference for section layout, loading/error states, formatting
READ src/hooks/useBurns.ts                        — data hook (BurnSummaryResponse)
READ src/lib/api/types.ts                         — BurnSummaryResponse type definition
READ src/lib/constants.ts                         — BURN_SOURCE, BURN_DESTINATION addresses
READ src/app/war-room/page.tsx                    — wire-in target
```

## Tasks

### Task 1: Create BurnOperations.tsx
**File:** `src/components/dashboard/BurnOperations.tsx` (CREATE)

**Structure:**
```
'use client'
imports: { useBurns } from hooks, { format, fromUnixTime } from date-fns, constants

LP_LOCKED_PCT = 100  // LP is 100% locked — static fact, not API-derived

Utility functions:
  - formatBurnAmount(n: number): string — comma-separated with 2 decimals
  - formatPct(n: number): string — 2 decimal places + %

Sub-components:

1. BurnSummaryBar — 3-cell grid:
   ┌─────────────────┬──────────────────┬──────────────────────────┐
   │ TOTAL INCINERATED│ % SUPPLY BURNED  │ % BURNED + LP LOCKED     │
   │ 12,345,678.00    │ 5.23%            │ 5.23% burned, 100% locked│
   └─────────────────┴──────────────────┴──────────────────────────┘
   - Loading: ████████ in burn accent color
   - Error: — in #ff9e9e

2. BurnTransactionsTable — scrollable table (max-h-[400px] overflow-y-auto):
   | DATE | AMOUNT | TX HASH |
   |------|--------|---------|
   | 2024-12-15 14:30 | 1,000,000.00 | abc123...def456 → Solscan ↗ |

   - Date: format(fromUnixTime(timestamp), 'yyyy-MM-dd HH:mm')
   - Amount: formatBurnAmount() with fire accent color
   - TX Hash: truncated (8...6), links to https://solscan.io/tx/{txHash}, target="_blank"
   - Empty state: "NO BURN TRANSACTIONS RECORDED"
   - Loading: 5 skeleton rows with ████████

3. Main BurnOperations export:
   - Section wrapper: <section className="w-full border-b border-[#333] bg-[#0d0d0d]">
   - Section header: "BURN OPERATIONS" left, "INCINERATED" right badge
   - INCINERATED visual treatment:
     - Section border-t accent: border-t-2 border-t-[#ff6b35] (orange-red)
     - Summary numbers in #ff6b35 instead of #d4f000
     - Subtle top glow: pseudo-element or gradient overlay in orange/red tones
     - "INCINERATED" badge: border border-[#ff6b35] text-[#ff6b35] with slight pulse animation
   - BurnSummaryBar
   - BurnTransactionsTable
```

**Acceptance:**
- Component renders with useBurns() data
- Loading states show redaction blocks
- Error states show dash fallbacks
- Solscan links open in new tab
- INCINERATED visual treatment visible (warm red/orange accent)
- Numbers formatted with commas
- Dates formatted correctly
- Table scrollable when many transactions

**Commit:** `feat(06-01): build BurnOperations.tsx with INCINERATED visual treatment`

### Task 2: Wire BurnOperations into war-room/page.tsx
**File:** `src/app/war-room/page.tsx` (MODIFY)

**Changes:**
1. Add import: `import BurnOperations from '@/components/dashboard/BurnOperations'`
2. Add `<BurnOperations />` below `<TreasuryIntel />` in the z-10 container
3. Update comment to indicate Phase 7+ sections

**Acceptance:**
- BurnOperations renders on /war-room page below TreasuryIntel
- No TypeScript errors
- Build passes (`next build`)

**Commit:** `feat(06-01): wire BurnOperations into war-room page`

### Task 3: Human-verify visual quality [CHECKPOINT]
**Type:** human_verify
**Instruction:** Start dev server, navigate to /war-room, verify:
- [ ] BurnOperations section visible below TreasuryIntel
- [ ] INCINERATED visual treatment (orange/red accent) distinguishes from green Treasury section
- [ ] Summary bar shows 3 stats (or loading/error states)
- [ ] Transaction table renders with proper formatting
- [ ] Solscan tx links work (open new tab)
- [ ] Section matches overall dark ops aesthetic
- [ ] Mobile responsive (single column on narrow viewport)

## Deviation Protocol
- If useBurns() returns unexpected shape → check types.ts and route.ts, adapt component
- If Recharts needed for burn chart → skip chart (not in R3 requirements), note for future
- If LP locked percentage needs to be dynamic → add API call later, hardcode 100% for now
