---
phase: 01-project-setup-dependencies
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
autonomous: true
requirements: [R9, R10]

must_haves:
  truths:
    - "Unused 3D/animation packages are fully removed from dependencies"
    - "Solana, TanStack Query, recharts, and date-fns are installed and resolvable"
    - "npm install completes without errors"
    - "next build succeeds with new dependencies and removed packages"
  artifacts:
    - path: "frontend/package.json"
      provides: "Updated dependency manifest"
      contains: "@solana/web3.js"
    - path: "frontend/package.json"
      provides: "Removed unused packages"
      not_contains: "@react-three/fiber"
  key_links:
    - from: "frontend/package.json"
      to: "frontend/node_modules"
      via: "npm install"
      pattern: "@solana/web3.js.*1\\.9"
---

<objective>
Clean up unused 3D/animation dependencies and install the Solana ecosystem + data visualization stack required for the War Room dashboard.

Purpose: The existing project has ~200MB+ of unused Three.js and Framer Motion packages that bloat the build. The War Room needs Solana RPC libraries, TanStack Query for async data management, recharts for visualizations, and date-fns for timestamp formatting.

Output: A clean package.json with only needed dependencies, all new packages installed and resolvable.
</objective>

<execution_context>
@.planning/phases/01-project-setup-dependencies/01-RESEARCH.md
</execution_context>

<context>
@.planning/phases/01-project-setup-dependencies/01-RESEARCH.md

The source project lives at the `pinky-and-the-brain-app` directory (sibling to this workspace on Desktop). It is a Next.js 16.2.1 app with React 19.2.4, Tailwind CSS v4, and Turbopack.

CRITICAL ARCHITECTURE DECISION (from orchestrator): The next.config.ts is EMPTY — no `output: 'export'`. This means the app runs in full Next.js server mode. API routes work as live request proxies at runtime. API keys do NOT need NEXT_PUBLIC_ prefix. This overrides the research document's static export assumption.

The existing package.json has these dependencies to REMOVE:
- @react-three/fiber, @react-three/drei, three (+ @types/three)
- framer-motion
- @studio-freight/lenis (if unused — verify)
- gsap (if unused — verify; WarRoom.tsx uses it, so KEEP)
- lenis (if unused — verify)

The existing package.json has these dependencies to ADD:
- @solana/web3.js@^1.98.4 (pinned to v1, NOT v2/@solana/kit)
- @solana/spl-token@^0.4.14
- @tanstack/react-query@^5
- recharts@^3.8.0
- date-fns@^4
- @tanstack/react-query-devtools@^5 (devDependency)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove unused 3D and animation packages</name>
  <files>frontend/package.json, frontend/package-lock.json</files>
  <action>
    In the source project directory (pinky-and-the-brain-app on Desktop), run:

    1. First, verify which packages are actually imported in source files. Search all .tsx/.ts files in src/ for imports of:
       - @react-three (any subpath)
       - three (direct import)
       - framer-motion
       - @studio-freight/lenis
       - lenis
       Check that BlueprintScene.tsx does NOT actually import Three.js (research says it compiles to video-background only).

    2. Remove confirmed unused packages:
       ```bash
       npm uninstall @react-three/fiber @react-three/drei three framer-motion --legacy-peer-deps
       ```
       Also remove @types/three from devDependencies:
       ```bash
       npm uninstall @types/three --legacy-peer-deps
       ```

    3. Check if @studio-freight/lenis and lenis are imported anywhere. If they are only used by SmoothScrolling.tsx (which IS active in layout.tsx), KEEP them. If they have zero imports, remove them too.

    4. Run `npm install --legacy-peer-deps` to ensure lock file is consistent.

    DO NOT remove gsap — it is actively used by WarRoom.tsx for scroll animations.
    DO NOT remove @studio-freight/lenis or lenis if SmoothScrolling.tsx imports them.
  </action>
  <verify>
    <automated>cd frontend && node -e "const pkg = require('./package.json'); const banned = ['@react-three/fiber','@react-three/drei','three','framer-motion','@types/three']; const found = banned.filter(b => (pkg.dependencies||{})[b] || (pkg.devDependencies||{})[b]); if(found.length) { console.error('FAIL: still present:', found); process.exit(1); } console.log('PASS: all unused packages removed');"</automated>
    <manual>Review package.json to confirm no Three.js or Framer Motion references remain</manual>
  </verify>
  <done>package.json has zero references to @react-three/fiber, @react-three/drei, three, @types/three, or framer-motion in dependencies or devDependencies</done>
</task>

<task type="auto">
  <name>Task 2: Install Solana ecosystem and data visualization dependencies</name>
  <files>frontend/package.json, frontend/package-lock.json</files>
  <action>
    In the source project directory, install the new packages:

    1. Install production dependencies:
       ```bash
       npm install @solana/web3.js@^1.98.4 @solana/spl-token@^0.4.14 @tanstack/react-query@^5 recharts@^3.8.0 date-fns@^4 --legacy-peer-deps
       ```

    2. Install dev dependencies:
       ```bash
       npm install -D @tanstack/react-query-devtools@^5 --legacy-peer-deps
       ```

    3. Verify installation by checking that each package resolves:
       ```bash
       node -e "require('@solana/web3.js'); require('@solana/spl-token'); require('@tanstack/react-query'); require('recharts'); require('date-fns'); console.log('All packages resolve successfully');"
       ```

    IMPORTANT version pins:
    - @solana/web3.js MUST be ^1.98.4 (v1 branch, NOT v2/@solana/kit). The npm `latest` tag now points to v2 which has incompatible API.
    - recharts MUST be ^3.8.0 (v3 has native React 19 support; v2 requires react-is override)
    - Use --legacy-peer-deps because React 19.2.4 is newer than some peer dep declarations.
  </action>
  <verify>
    <automated>cd frontend && node -e "const pkg = require('./package.json'); const needed = {'@solana/web3.js':'1','@solana/spl-token':'0','@tanstack/react-query':'5','recharts':'3','date-fns':'4'}; const missing = []; for(const [name,major] of Object.entries(needed)) { const v = (pkg.dependencies||{})[name]; if(!v) { missing.push(name+' not in deps'); } else if(!v.includes(major)) { missing.push(name+' wrong major: '+v); } } const devNeeded = ['@tanstack/react-query-devtools']; for(const name of devNeeded) { if(!(pkg.devDependencies||{})[name]) missing.push(name+' not in devDeps'); } if(missing.length) { console.error('FAIL:', missing.join(', ')); process.exit(1); } console.log('PASS: all packages installed with correct versions');"</automated>
  </verify>
  <done>All five production packages (@solana/web3.js v1, @solana/spl-token, @tanstack/react-query v5, recharts v3, date-fns v4) are in dependencies. @tanstack/react-query-devtools is in devDependencies. All resolve without errors.</done>
</task>

<task type="auto">
  <name>Task 3: Verify build succeeds with updated dependencies</name>
  <files>frontend/package.json</files>
  <action>
    Run `next build` in the source project directory to confirm the dependency changes do not break the existing site:

    ```bash
    cd frontend && npx next build
    ```

    If the build fails due to imports of removed packages (e.g., BlueprintScene.tsx importing from three or framer-motion):
    1. Check the exact import causing the failure
    2. If BlueprintScene.tsx imports Three.js: replace the import with a comment noting it was removed, keep the component functional (it should be video-background only per research)
    3. If any component imports framer-motion: replace with CSS transitions or remove the animation

    If the build fails due to Turbopack polyfill issues with @solana/web3.js:
    - Do NOT add @solana/web3.js imports to any existing files yet — that is Phase 2+ work
    - The packages just need to be installed; they are not imported anywhere yet

    The build should succeed because we only removed unused packages and added new ones that are not yet imported.
  </action>
  <verify>
    <automated>cd frontend && npx next build 2>&1 | tail -5</automated>
    <manual>Build completes with exit code 0, no errors about missing modules</manual>
  </verify>
  <done>next build completes successfully with exit code 0. No import errors for removed packages. No polyfill errors from new packages (since they are not yet imported).</done>
</task>

</tasks>

<verification>
1. `npm ls @solana/web3.js` shows v1.98.x installed
2. `npm ls recharts` shows v3.x installed
3. `npm ls @react-three/fiber` returns "not installed" or errors (expected)
4. `npx next build` succeeds
</verification>

<success_criteria>
- Unused 3D packages fully removed (saves ~200MB in node_modules)
- All 5 new production deps installed at correct major versions
- DevTools installed as dev dependency
- Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup-dependencies/01-01-SUMMARY.md`
</output>
