---
phase: 05-treasury-intel-section
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/app/war-room/page.tsx
autonomous: false
requirements: [R2, R10]

must_haves:
  truths:
    - "Visiting /war-room shows TreasuryIntel section below CommandHeader"
    - "TreasuryIntel section is visible without scrolling errors or layout breaks"
    - "The classified aesthetic is consistent between CommandHeader and TreasuryIntel"
    - "Holdings grid loads with redaction blocks then populates with live data"
    - "Recharts chart renders with correct colors (no white/default chart theme)"
    - "Next.js production build completes without errors"
  artifacts:
    - path: "src/app/war-room/page.tsx"
      provides: "War Room page with TreasuryIntel wired in"
      contains: "TreasuryIntel"
  key_links:
    - from: "src/app/war-room/page.tsx"
      to: "src/components/dashboard/TreasuryIntel.tsx"
      via: "import TreasuryIntel from '@/components/dashboard/TreasuryIntel'"
      pattern: "import TreasuryIntel"
---

<objective>
Wire TreasuryIntel into the War Room page and visually verify the full section renders correctly.

Purpose: Phase 5 is complete only when TreasuryIntel is live in the page and human-verified as visually correct. The component built in 05-01 needs a one-line import + placement in page.tsx, then a human check to confirm aesthetic quality.

Output: Updated src/app/war-room/page.tsx with TreasuryIntel slotted below CommandHeader, plus human visual approval.
</objective>

<execution_context>
@C:/Users/lucid/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lucid/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-treasury-intel-section/05-01-SUMMARY.md

# Source files
@src/app/war-room/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire TreasuryIntel into war-room/page.tsx and verify build</name>
  <files>src/app/war-room/page.tsx</files>
  <action>
Read src/app/war-room/page.tsx first to understand the current slot structure (from Phase 4, it has a {/* Phase 5: TreasuryIntel */} slot comment or similar).

Add the TreasuryIntel import and component slot:

```typescript
import TreasuryIntel from '@/components/dashboard/TreasuryIntel'
```

Place `<TreasuryIntel />` directly below `<CommandHeader />` in the page JSX, inside the `<div className="relative z-10">` wrapper. Remove any placeholder comment that was there for Phase 5.

page.tsx MUST remain a Server Component (no 'use client' directive). TreasuryIntel is the 'use client' leaf — that boundary is already correct.

After wiring, run the Next.js build to confirm no build errors:

```bash
PROJ="$(realpath ../../../../)/Brain Website Build" && powershell.exe -NoProfile -ExecutionPolicy Bypass -File "$(wslpath -w "$PROJ/run.ps1")" "npx next build"
```

If build fails due to Recharts server-side import (Recharts must be client-only), check that TreasuryIntel.tsx has 'use client' at the top. The 'use client' directive in TreasuryIntel is the boundary — Recharts will be bundled client-side only. Do not move Recharts imports to page.tsx.

If build fails due to any other reason, diagnose and fix in TreasuryIntel.tsx — do not modify page.tsx beyond the import + slot addition.
</action>
  <verify>
    <automated>PROJ="$(realpath ../../../../)/Brain Website Build" &amp;&amp; powershell.exe -NoProfile -ExecutionPolicy Bypass -File "$(wslpath -w "$PROJ/run.ps1")" "npx next build"</automated>
    <manual>Confirm page.tsx imports TreasuryIntel and places it below CommandHeader. Page remains a Server Component (no 'use client' at top of page.tsx).</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Next.js build completes with exit code 0. page.tsx imports TreasuryIntel and renders it below CommandHeader. page.tsx has no 'use client' directive.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    TreasuryIntel section wired into /war-room page. Includes:
    - Summary bar (4 stats: total USD, SOL balance, portfolio SOL, holdings count)
    - Holdings grid with HoldingCard (known tokens) and CLASSIFIED redacted cards (unknown tokens)
    - Treasury Value Over Time Recharts AreaChart (or "INSUFFICIENT DATA" fallback if fewer than 2 dated holdings)
    - Divested Assets subsection (empty state or list)
    - All loading states use redaction blocks (████████)
    - Copy-to-clipboard on contract addresses
    - Solscan and Bags.fm external links
  </what-built>
  <how-to-verify>
    1. Start the dev server: run `npm run dev` in the project directory
    2. Visit http://localhost:3000/war-room
    3. Confirm TreasuryIntel section appears below the CommandHeader command bar
    4. Verify initial load shows redaction blocks (████████) then replaces with live data
    5. Check holdings cards: known tokens show name/value/gain-loss; unknown tokens show CLASSIFIED with flicker
    6. Click a contract address copy button — confirm "COPIED" state appears for 2 seconds
    7. Click a SOLSCAN link — confirm opens Solscan in a new tab
    8. Verify chart area: either renders AreaChart with #d4f000 (yellow-green) line/fill, OR shows "INSUFFICIENT DATA FOR CHART" in classified style
    9. Confirm color scheme matches CommandHeader exactly (dark bg, #d4f000 accent, monospace fonts, border-[#333])
    10. Check no layout breaks on mobile viewport (single column grid)
  </how-to-verify>
  <resume-signal>Type "approved" if the section looks correct, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
Build verification:
```bash
PROJ="$(realpath ../../../../)/Brain Website Build" && powershell.exe -NoProfile -ExecutionPolicy Bypass -File "$(wslpath -w "$PROJ/run.ps1")" "npx next build"
```

Must pass with exit code 0 before the human checkpoint.
</verification>

<success_criteria>
- Next.js build succeeds (exit code 0)
- /war-room page renders TreasuryIntel below CommandHeader
- Human approves visual quality: classified aesthetic consistent, loading states correct, data populates, links work
- page.tsx remains a Server Component
</success_criteria>

<output>
After completion, create `.planning/phases/05-treasury-intel-section/05-02-SUMMARY.md`
</output>
